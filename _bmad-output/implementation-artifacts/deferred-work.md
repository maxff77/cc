# Deferred Work

## Deferred from: code review of 1-6-reset-de-contrasena-con-cambio-forzado (2026-06-11)

- Generated API types document only 200/422 responses — the 400/401/403/404 error codes the frontend routes on (`password_reuse`, `password_change_required`, `forbidden`, `user_not_found`) are untyped in `frontend/types/api.ts`. Pre-existing FastAPI/openapi-generator behavior across all stories; fixing it means declaring `responses=` on every route, not a 1.6 concern. **ACCEPTED (2026-06-12, cierre):** deuda conocida a nivel proyecto; el contrato `{code,message}` runtime está cubierto por `toApiError` — sin impacto funcional.

## Deferred from: code review of 1-7-despliegue-en-produccion-con-https-y-re-auth-de-telegram-en-el-vps (2026-06-11)

- ~~Caddy `handle /ws` is exact-match (no `/ws/*`) and bare `/api` falls through to the Next.js catch-all — the real WebSocket path shape is unknown until Story 2.2 ships the endpoint; widen the matcher then (`deploy/Caddyfile`).~~ **RESOLVED in Story 2.2 (2026-06-11):** the endpoint is exactly `/ws`, so the existing exact `handle /ws` matcher is correct as-is — no Caddy change needed.
- deploy.sh partial-failure window: if `npm run build` fails after `alembic upgrade head`, the old backend keeps running on the migrated schema with no rollback path — accepted at MVP scale (additive migrations) (`deploy/deploy.sh`). **ACCEPTED (2026-06-12, cierre):** decisión de diseño MVP registrada; sin cambio.
- ~~telegram_auth.py: a corrupt/foreign pre-existing session file produces a raw sqlite/Telethon traceback with no delete-and-rerun hint — rare; the full re-auth runbook is Story 4.4's deliverable (`backend/scripts/telegram_auth.py`).~~ **RESOLVED via Story 4.4 (2026-06-12):** `docs/runbooks/reauth-telegram.md` documenta el procedimiento completo incluido delete-and-rerun.
- ~~AC4 pending owner action: `anon.session` absent in production — interactive re-auth (phone→code→2FA) only Richard can run; commands in story 1.7 Review Findings. Blocks story 1.7 → done.~~ **RESOLVED (2026-06-12):** re-auth ejecutado en el VPS (sesión `/var/lib/cc/anon.session`, `-rw------- cc cc`, autorizada como @maxff9778; `TELEGRAM_TARGET` configurado). Story 1.7 → done.

## Deferred from: code review of 2-1-catalogo-global-de-prefijos (2026-06-11)

- Hand-written `GateOut`/`GateListResponse` interfaces in `frontend/app/admin/gates/page.tsx` instead of consuming generated `types/api.ts` — mirrors the users-page idiom from story 1.3, contradicts architecture.md "never hand-write API types"; migrate all admin pages to generated types in one epic-wide pass. **ACCEPTED (2026-06-12, cierre):** deuda conocida; refactor mecánico de bajo valor frente al riesgo de tocar páginas recién restilizadas — candidato a chore post-lanzamiento.
- Delete-confirm copy on /admin/gates: spec self-conflicts (Task 6 "¿Eliminar este gate? ({value})" — implemented — vs EXPERIENCE.md "¿Eliminar este gate? No se puede deshacer."); owner will set the final wording himself later (`frontend/app/admin/gates/page.tsx`). **PENDIENTE DEL OWNER (2026-06-12, cierre):** decisión de copy explícitamente reservada a Richard; sin cambio.

## Story 2-2 review (deferred)

- ~~MEDIUM — `backend/migrations/versions/b8e52d0cf1a4_batches_and_batch_lines.py:23`: the "one live batch per tenant" invariant has no DB enforcement — two concurrent POSTs (two tabs; isPending guard is per-tab) both see no live batch and create two state='sending' batches, then `get_live_batch` uses `.first()` with no ORDER BY, so snapshot/append target a nondeterministic batch (AC 10 violated). Fix: partial unique index `CREATE UNIQUE INDEX uq_batches_one_live_per_tenant ON batches (tenant_id) WHERE state = 'sending'`; catch IntegrityError in the route and retry as append; add `.order_by(Batch.id)` to `get_live_batch`. Note for 2.3: widen the index predicate when 'paused'/'stopping' arrive.~~ **RESOLVED in Story 2.3 (2026-06-12):** migration `1b606109cc99` adds the partial unique index with the widened predicate `state IN ('sending','paused','stopping')` (+ pre-clean of any live duplicates), `get_live_batch` orders by id, and the POST handler converts the IntegrityError into an append.
- ~~MEDIUM — `backend/app/api/batches.py:121`: two concurrent appends both read `next_position()` = max+1 and collide on `uq_batch_lines_batch_id_position` → unhandled IntegrityError → raw 500 (breaks {code,message} contract), that request's lines lost. Largely covered by the new FOR UPDATE lock on the live batch row (serializes appenders); alternatively catch IntegrityError around commit and retry the append once.~~ **RESOLVED via Story 2.3 (2026-06-12, verificado al cierre):** el FOR UPDATE sobre el batch vivo serializa los appenders — la colisión de posición ya no es alcanzable por la ruta.
- ~~MEDIUM — `backend/app/api/ws.py:66`: snapshot is built/sent BEFORE `broadcaster.register` (line 70), so events in that gap are dropped for the socket. If the dropped frame is the terminal `batch.state {state:'idle'}`, the tab shows 'sending' with a stale ring indefinitely (breaks AC 11's silent reconcile). Fix: register before building the snapshot (keep unregister in finally), or buffer events until the snapshot is flushed.~~ **RESOLVED in Story 2.3 (2026-06-12):** `/ws` now registers before building/sending the snapshot; unregister stays in the `finally`.
- ~~MEDIUM — `backend/app/api/ws.py:54`: auth checked only at handshake — a blocked user / revoked session / expired plan keeps receiving live tenant events (incl. full message text) on an already-open socket; Story 1.5's "loses access" is bypassed for the live stream. Fix: track sockets by user/session in the Broadcaster and close from block/revoke paths, or re-run `resolve_ws_user` periodically in the receive loop and close(4401) on failure.~~ **RESOLVED (2026-06-12, cierre):** el receive loop re-corre `resolve_ws_user` cada 60s (`_REAUTH_SECONDS`) y cierra 4401 — ventana de exposición acotada a un minuto.
- ~~MEDIUM — `backend/app/core/send_worker.py:84`: if the step-3 recording transaction fails transiently AFTER `gateway.send` succeeded, the exception bubbles to `run_worker`'s catch and the worker moves to the NEXT line — the sent line stays 'sending' forever (batch never completes, ring stuck at n-1/n, queued>=1 forever) until restart (which then double-sends it). Fix: bounded retry loop around the idempotent recording block, or have `next_queued_line` also claim 'sending' lines older than a threshold.~~ **RESOLVED in Story 2.5 (2026-06-12):** the post-send record phases (`_record_sent`/`_record_failed`) retry FOREVER until the DB takes the idempotent transaction (write-ahead `send_log` row + line UPDATE) — that retry IS the AC 5 fail-stop ("no attribution possible = no sends"); and even an unhealed crash no longer double-sends thanks to boot reconciliation against recent outgoing messages. Covered by `tests/test_send_hardening.py` (record-retry + reconciliation tests).
- ~~MEDIUM — `backend/app/api/batches.py:95`: an already-connected idle tab never learns gate_name/gate_value when a batch starts — `batch.state` carries only `{state:'sending'}` and `batch.progress` only counts; gate fields exist only in the connect-time snapshot. Second tab renders "Gate activo" chip as " · " and send-form's liveGateId falls back to gates[0] (AC 8 multi-tab). Fix: include gate_name/gate_value/batch_id in the new-batch `batch.state` emission and consume them in the `batch.state` reducer in `frontend/lib/ws.ts`.~~ **RESOLVED in Story 2.3 (2026-06-12):** every `batch.state` emission now uses `services/batches.state_data` (`{batch_id, state, gate_name, gate_value}`) and the `batch.state` reducer consumes the new fields.
- ~~LOW — `backend/app/api/admin.py:466` (and update_gate :509): IntegrityError is unconditionally mapped to gate_exists (409) — if the category is deleted concurrently between `_require_category` and commit, the failing constraint is the FK `fk_gates_category_id_gate_categories`, yielding a misleading "duplicate gate" error. Fix: inspect the constraint name on `exc.orig` and map the FK violation to `category_not_found()`.~~ **RESOLVED (2026-06-12, cierre):** ambos handlers inspeccionan `exc.orig` y mapean la violación de FK a `category_not_found()`.
- ~~LOW — `frontend/lib/ws.ts:197`: `seedFromBatch` can regress fresher WS state — backend emits `batch.progress` (and may send line 1) before the HTTP response returns, then onSuccess seeds sent=0, rolling the ring backwards for up to one send interval. Fix: in seedFromBatch, return early when `store.batchId === batch.id`; only seed when flipping from idle/another batch.~~ **RESOLVED in Story 3.2 (2026-06-12):** `seedFromBatch` now returns early when `store.batchId === batch.id` — the seed only flips from idle/another batch and never regresses fresher WS state (and the 3.2 session fields are preserved across seeds).

## Story 2-3 review (deferred)

- ~~MEDIUM — `backend/app/core/send_worker.py:50`: `wake()` is one global event consumed by whichever sleep the lone worker is in; 2.3 adds the first callers (pause/resume/stop in `api/batches.py`). Cross-tenant collateral: (1) tenant A's control cuts tenant B's in-flight FloodWait sleep — the re-check sees B still 'sending' and retries `gateway.send` on the SHARED account before Telegram's window elapsed, typically earning another (escalating) FloodWait; (2) the same wake cuts the inter-send interval sleep in `run_worker`, so alternating pause/resume drives the global send rate past the system interval (FR12 pacing bypass) for everyone. Fix: deadline loop — record `deadline = now + seconds` before `sleep_cancelable`; after a wake-interrupted sleep, if the re-read state is still 'sending' (wake was for another batch) re-sleep the remainder instead of retrying; same remainder re-sleep for the post-send interval (pause still lands instantly since a paused batch isn't served). Legacy pause→resume-retries-early semantics survive via release/re-claim.~~ **RESOLVED in Story 2.4 (2026-06-12):** `_wait_respecting_state` (FloodWait/error waits) re-sleeps the remainder when the re-read state is still 'sending' (own pause/stop still land instantly → release/abort), and the post-send pacing sleep is `sleep_paced` — a deadline loop immune to `wake()` altogether (FR12 never bypassed). Covered by `tests/test_scheduler.py` (foreign-wake re-sleep + wake-immune pacing tests).
- ~~MEDIUM — `frontend/components/client-nav.tsx:34`: the 'En pausa' pill is unreadable — `PILL_CLASS.paused` uses `bg-warning/18 text-warning-foreground`, but `--warning-foreground` is near-black (contrast color for SOLID warning fills) and the app hardcodes dark mode, so it renders near-black 10px text on a near-black amber tint. Siblings do it right (`text-accent`, `text-danger`). Fix: `bg-warning/18 text-warning`.~~ **RESOLVED in Story 3.3 (2026-06-12):** `PILL_CLASS.paused` is now `bg-warning/18 text-warning` (the exact one-word fix, mirroring its siblings) — absorbed by 3.3 because its nav change (Historial active on `/sessions/[id]`) touches the same file.
- ~~LOW — `frontend/components/batch/flood-notice.tsx:20`: the 1s `setInterval` never stops after the countdown expires while `floodUntil` stays non-null — `flood.wait` is GLOBAL but the clearing signals are tenant-scoped to the sender, so an idle tenant's store keeps a stale `floodUntil` and the interval re-renders every FloodNotice consumer once per second until a reconnect snapshot resets it. Fix: clear the interval at expiry (e.g. `if (Date.now() >= until) window.clearInterval(id)`) or a store action that nulls `floodUntil` on expiry so the existing `=== null` early-returns kick in.~~ **RESOLVED in Story 2.4 (2026-06-12):** the effect captures `until` in the closure and the interval callback self-clears once `Date.now() >= until` (the `seconds <= 0` render guard already hides the strip); render/copy/amber styling untouched.

## Story 2-4 review (deferred)

- ~~MEDIUM — `backend/app/core/send_worker.py:235`: cross-tenant FloodWait-window bypass via the release/abort path — tenant A's FloodWait wait returns 'release'/'abort' on A's pause/stop, `step()` returns False, the worker sleeps only `_IDLE_SLEEP_SECONDS` (1s) and then sends ANOTHER tenant's line ~1s into Telegram's open window on the shared account, escalating the next FloodWait for everyone (the 2.3 deferred #1 scenario; the recorded legacy decision only covers the SAME tenant's pause→resume early retry). The deadline is per-claimed-wait, never global. Fix: track the window globally in the scheduler — `note_flood_wait(seconds)` records `self._flood_until = self._now() + seconds`, expose `flood_remaining() -> float`, and in `run_worker`/top of `step()` `await sleep_paced(scheduler.flood_remaining())` before claiming (optionally exempting the window-owning tenant to preserve same-tenant resume-early semantics).~~ **RESOLVED in Story 2.5 (2026-06-12):** `note_flood_wait(seconds)` now records the global window (`flood_remaining()`), the top of `step()` sleeps it out before claiming ANYONE's line, and — recorded decision, no exemption — the window applies to the owning tenant too (pause→resume no longer retries inside an open window; pause/stop themselves still land instantly). Covered by `tests/test_send_hardening.py` (window unit + survives-release + gates-next-claim tests).
- ~~LOW — `_bmad-output/implementation-artifacts/sprint-status.yaml:75`: tracking inconsistency — the 2-4 story file says `Status: review` with a completed Dev Agent Record, but development_status still shows `ready-for-dev`. Fix: advance the 2-4 entry to `review` and add the matching `# 2-4 → review 2026-06-12 (...)` history comment.~~ **OBSOLETE (2026-06-12):** the tracking already advanced — sprint-status.yaml shows 2-4 `done` with its history comment; nothing left to fix.
- ~~LOW — `.agents/` + `skills-lock.json`: untracked Claude Code skill-manager artifacts (caveman, cavecrew, etc.) sit in the working tree alongside the story changes — a `git add -A` story commit would check developer-machine tooling state into the product repo. Fix: add `.agents/` and `skills-lock.json` to `.gitignore` (or otherwise exclude them from the story commit).~~ **RESOLVED in Story 2.5 (2026-06-12):** both entries added to `.gitignore` (dev-machine tooling stays out of the product repo).

## Story 2-5 review (deferred)

- ~~MEDIUM — `backend/app/core/telegram.py:111`: boot reconciliation (AC 6 "never double-sent") rests on a fragile match. (1) `iter_messages(..., from_user="me")` makes Telethon use messages.Search instead of getHistory — weaker consistency for a message sent seconds before a crash; if it's missing from the result the line is re-queued and double-sent. (2) The worker compares raw `line.text` against `message.text`, which is Telethon's re-rendered markdown (default parse_mode='md'): lines with non-round-tripping chars (`**`, `__`, backticks, `[..](..)`) fail the equality check and re-queue a delivered line — and are delivered to the bot with those chars stripped. Fix: in `recent_outgoing` drop `from_user="me"` and iterate plain history filtering `m.out` client-side (getHistory is strongly consistent); send with `parse_mode=None` so delivered text == line.text byte-for-byte (also stops corrupting data lines containing markdown delimiters).~~ **RESOLVED (2026-06-12, cierre):** `send()` usa `parse_mode=None` (texto entregado == line.text byte a byte) y `recent_outgoing` itera historia plana (getHistory) con filtro `m.out` + `raw_text`, ventana ×4 con corte en `limit` salientes.
- ~~LOW — `backend/app/core/send_worker.py:616`: reconciliation's confirm path assumes the send_log intent row exists — `set_message_id` is a bare UPDATE keyed on line_id and silently no-ops without a row. Lines left 'sending' by a crash BEFORE this deploy (first boot after the 2.5 migration) have no intent row → confirmed line ends 'sent' with no send_log record (Story 3.1 can't attribute replies) and its message_id is invisible to `used_message_ids`, letting an identical-text old message falsely confirm a future stuck line. Fix: in `_boot_recovery`'s confirm branch, `record_intent` (idempotent) before `set_message_id` — or make `set_message_id` upsert.~~ **RESOLVED in Story 3.1 (2026-06-12):** `_boot_recovery`'s confirm branch now calls `record_intent` (idempotent get-or-create) before `set_message_id`, so a pre-2.5 stuck line confirmed at boot gets a real `send_log` row (attributable + visible to `used_message_ids`). Covered by `tests/test_attribution.py::test_boot_recovery_confirm_creates_missing_intent_row`.
- ~~LOW — `backend/app/core/send_worker.py:398`: `_cancel_expired_batch` sets STATE_CANCELLED without re-checking state under the FOR UPDATE lock. Between the expiry check and the cancel transaction a user stop can finalize the batch 'stopped' (terminal, queued lines deleted); the cancel then rewrites it 'cancelled' (cancelled=0), misrepresenting a user stop as system cancellation in Epic 3 history and emitting a redundant idle `batch.state`. Fix: after `_locked_batch`, bail unless `batch.state in batches_repo.LIVE_STATES` (lock makes it race-free; paused→cancelled for an expired tenant stays intentional).~~ **RESOLVED (2026-06-12, cierre):** re-check `LIVE_STATES` bajo el lock — el fix propuesto, literal.
- ~~LOW — `frontend/lib/ws.ts:162`: the `batch.line_failed` reducer ignores the payload's `batch_id` — it appends to whatever batch the store holds and dedups only by `position`. A stale/out-of-order frame racing a `seedFromBatch` of a new batch could attach another batch's failed line, and position-collision dedup would then suppress a legitimate failure of the new batch at the same position. Fix: guard like the other scoped reducers: `if (store.batchId !== null && store.batchId !== d.batch_id) break;`.~~ **RESOLVED in Story 3.2 (2026-06-12):** the `batch.line_failed` reducer now guards on `batch_id` before the position dedup — exactly the proposed one-liner, mirror of the other scoped reducers.
- ~~LOW — `backend/app/core/send_worker.py:652`: the new fail-stop (retry-forever) covers only the record phases; `_release_line`/`_abort_line` still let a DB failure bubble to `run_worker`, whose catch logs `event=db_unreachable phase=claim` ("nothing was sent") — then the loop claims OTHER lines while the released/aborted line stays 'sending' forever (batch can't drain or finalize until next restart's reconciliation; >1-stuck-lines invariant violated meanwhile). Pre-existing gap papered over by the new framing. Fix: apply the same retry loop to `_release_line`/`_abort_line` (both idempotent), or at minimum log a distinct `phase=release|abort`.~~ **RESOLVED (2026-06-12, cierre):** retry-forever aplicado a ambos (idempotentes) con logs `phase=release|abort` distintos.
- ~~LOW — `backend/app/core/send_worker.py:28`: AC 5's second half ("incoming replies buffer in memory ... flush on DB recovery") is not implemented — only documented as a recorded decision in the module docstring (no capture handlers exist until Story 3.1; `catch_up=True` covers disconnection replay). Literal AC text fails; the deferral must be consciously accepted. Fix: no code change if the decision stands; ensure Story 3.1's spec explicitly inherits the DB-down in-memory reply buffer (mirror the worker docstring note in 3.1's story file) or amend AC 5's wording in epics.md.~~ **RESOLVED in Story 3.1 (2026-06-12):** the capture pipeline's blocked `asyncio.Queue` + single retry-forever consumer (`core/capture.py`) IS the in-memory buffer — with the DB down, replies accumulate in the queue and flush IN ORDER on recovery (`catch_up=True` keeps covering Telegram-side disconnections). Covered by `tests/test_attribution.py::test_capture_buffers_replies_while_db_down_and_flushes_in_order`.

## Ranger-X Check neon reskin — screen goals deferred from the foundation (2026-06-13)

Context: the Claude Design handoff (`Ranger-X Check`, bundle `g_hgmhSvvnImjlaxrydmFw` / `8lXkWvS_Ce4m3OaKh2gSKA`) is a full-frontend visual redesign on a native-CSS token system + custom React primitives — **zero HeroUI components**. Owner decisions (2026-06-13): implement **native + tokens** (not HeroUI), adopt the **full neon identity** (glow, circuit backdrop, gradient-clipped wordmark, Saira display font, cyan→violet→magenta, 12px radius — this **supersedes** the codebase's "sobria / no gradient-on-text / sharp radius" rules and the `[[rebrand-ranger-x]]` "sobria" memory). Multi-goal split: this cycle ships **the foundation only** (`spec-rangerx-design-foundation`). The screen reskins below are each an independently shippable PR that consumes that foundation and are **deferred to later cycles**:

- **App chrome / shell** — sticky `Chrome` header (logo `Mark` + RANGER-X gradient wordmark, nav tabs Envío/Historial/Usuarios/Flujos with gradient underline, theme toggle, Cerrar sesión) + wire `rx-backdrop` onto the authenticated `(client)`/`admin` layouts. Replaces `components/client-nav.tsx`.
- **Login + auth states** — `LoginScreen` (branded card with `--brand-gradient-soft` bg, `CornerTicks`, user/pass fields, Recordarme, WhatsApp/Telegram contact) + `AuthLayout`/`ContactPanel`-based `change-password`, `expired`, `blocked`, `error`. Replaces the current HeroUI `app/login/page.tsx` (the uncommitted "sobria" WIP) and the `app/change-password|expired|error` pages.
- **Envío cockpit** — `ProgressRing` + metrics, "Sesión activa" rail card, Controles (pausar/reanudar/detener), "Nuevo lote" (Categoría→Flujo `Select`s + `Area`), and the 3 `ResultPanel`s (Completa / Filtrada con respuesta / Filtrada sin respuesta). Reskins `app/(client)/page.tsx` + `components/batch/*` + `components/sessions/response-views.tsx`.
- **Historial + Detalle** — sessions grouped by gate (`SectionCard` legend + `legendRight` mono chip, continuar/renombrar/eliminar) and the 3-panel session detail with back nav. Reskins `app/(client)/sessions/*`.
- **Usuarios (admin)** — segmented Crear cliente/admin switch, create form, Control de admisión, users table with role/estado pills + per-row actions. Reskins `app/admin/users/*`.
- **Flujos / Gates (admin)** — categories + gates list (`SectionCard` per category, rename/delete, Nuevo flujo / Categoría actions). Reskins `app/admin/gates/*`.

Foundation review notes (carry into the chrome/Logo goal):
- **Saira condensed**: `components/ui/logo.tsx` sets `fontStretch: "condensed"` on the wordmark, but `config/fonts.ts` loads Saira as STATIC instances (`weight: ["700","800"]`), which carry no `wdth` axis — so the wordmark renders normal-width, not condensed (the handoff prototype had the same limitation). When the Logo actually ships in the nav/login, load Saira as a variable font with the `wdth` axis (or a `Saira_SemiCondensed` family) if true condensed width is wanted.

## Concurrency-scaled send interval — deferred from quick-dev split (2026-06-13)

**RESOLVED (2026-06-13) via `spec-flat-send-interval` (branch `feat/flat-send-interval`, commit 5deb86c).** Owner clarified the real intent: a CONSTANT 4s cadence with round-robin spreading the slot across clients (not an inverted curve). Implemented as flat `interval() == g_min`, default 3.0 → 4.0s; the band/`P(n)`/`_target_per_client` were removed. "More clients = each slower" now falls out of round-robin (turn = G×n) without slowing the account. The FloodWait governor stays the real ban protection. Suite green (318), load-test gate passes at g_min=4.0. The inversion question below was answered: NOT wanted — flat constant chosen. Original deferral context kept below for history.

Context: owner (Richard) asked for an interval that **scales with concurrency** — few active users → send FASTER, many → send SLOWER, with a HARD floor of **4s** ("de eso no puede bajar más"). Split out (2026-06-13) from the multi-target-send work because it touches a load-bearing safety/product constant and needs its own design decision; multi-target ships first.

**Conflict to resolve before building.** `core/scheduler.py` already adapts to concurrency, but in the OPPOSITE direction. Current formula `G = max(g_min, P(n)/n)` with `P(n)=min(20, 10+2.5·(n−1))` and `g_min` (`scheduler_g_min_seconds`, default **3.0**) yields: n=1→10s, n=2→6.25s, n=3→5s, n=4→4.38s, n=5→4s, n≥6→~3s (floored). So today **few users = SLOW (10s), many = FAST (3s)** — the reverse of the request. This is deliberate (FR13 / Story 2.4): the 10–20s band guarantees each client a turn within ≤20s, and the floor protects the shared account. The floor IS the real ban protection; the per-client band is what the owner wants inverted.

Open design questions for the spec:
- **Is the inversion actually wanted once explained?** The floor (raise 3.0→4.0) already caps the account's max send rate regardless of n; inverting the curve makes the app slowest exactly when busiest, for no extra account safety (the floor already bounds global rate). Confirm intent vs. just bumping the floor.
- If inverting: define the new curve (e.g. `G = clamp(4s, 4 + k·(n−1), ceiling)`), pick `k` and a ceiling so high-n doesn't starve clients, and reconcile with FR13 / `architecture.md` (the 10–20s band is documented as a non-configurable product constant — amend it).
- Trivial part regardless of direction: `scheduler_g_min_seconds` default 3.0 → **4.0** (`backend/app/config.py:63`) + `.env.example`.
- Interaction with the FloodWait governor (`note_flood_wait` raises `g_min` ×1.5, `_G_MIN_CEIL=30`) and lazy decay — keep both.
- "detectara automaticamente la concurrencia": n already = `active_senders` (non-paused) from the repo; reuse it, no new detection needed.

## Flaky test: test_flood_window_gates_the_next_claim — surfaced 2026-06-14

Surfaced during the `spec-configurable-send-interval` review (NOT caused by it — the acceptance auditor confirmed it fails at the same ~3/8 rate on the clean baseline `177d110` with the feature stashed out). `backend/tests/test_send_hardening.py:625` opens a real 0.4s FloodWait window (`scheduler.note_flood_wait(0.4)`) then asserts `await asyncio.wait_for(send_worker.step(), timeout=2.0) is True`. Under event-loop jitter against the wake-immune `sleep_paced` gate, `step()` occasionally observes `flood_remaining()==0` and returns `False`, failing the assert non-deterministically.

Fix options (separate, focused change): inject/fake the clock for the flood window (the scheduler already takes an injectable `now`), or widen the window well above the timeout. Keep the FloodWait/governor/window semantics — only the test timing is fragile.
